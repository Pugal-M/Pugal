// src/components/GlobalLoadingIndicator.tsx
'use client';

// Removed useLoading and Loader2 as the component will always return null.

export function GlobalLoadingIndicator() {
  // Returning null to remove the global loading indicator.
  return null;
}
